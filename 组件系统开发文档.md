# 组件系统开发文档

> 版本：1.0 · 适用：Start2 组件框架 (`core/widgets.py`)

---

## 目录

- [架构总览](#架构总览)
- [快速开始](#快速开始)
  - [本地组件](#1-本地组件--)
  - [联网组件](#2-联网组件--)
- [缓存系统](#缓存系统)
  - [基本配置](#基本配置)
  - [多缓存键](#多缓存键)
  - [JSON 路径读写](#json-路径读写)
  - [过期策略详解](#过期策略详解)
- [异步模式](#异步模式)
  - [短连接（默认）](#短连接默认)
  - [长连接（高频场景）](#长连接高频场景)
- [API 参考](#api-参考)
  - [WidgetBase](#widgetbase)
  - [LocalWidgetBase](#localwidgetbase)
  - [NetworkWidgetBase](#networkwidgetbase)
  - [MultiSourceWidgetBase](#multisourcewidgetbase)
  - [CacheManager](#cachemanager)
- [从旧版迁移](#从旧版迁移)
- [FAQ](#faq)

---

## 架构总览

```
WidgetBase                     ← get_data() / get_data_async() 模板方法
│                                 子类只需覆写 _fetch_data()
│                                 NEED_CACHE / LOCAL_INTERVAL 自动消费
│
├── LocalWidgetBase            ← 本地数据，不依赖网络
│     _(fetch_data)_
│
├── NetworkWidgetBase          ← 联网数据，封装 httpx 请求
│     ├─ _async_request()          ← 短连接（默认）
│     └─ _async_request_persistent()  ← 长连接（需手动 close）
│
└── MultiSourceWidgetBase      ← 多数据源联网，按配置切换数据源
      SOURCES + CONFIG_ITEM 定义可用数据源，由用户选择
```

**核心原则：** 子类声明意图（`WIDGET_NAME` / `NEED_CACHE` / `LOCAL_INTERVAL`），框架自动编排"读缓存 → 判断过期 → 重新获取 → 写回缓存"。

---

## 快速开始

### 1. 本地组件 —— 从 data.json 读取用户昵称

```python
from datetime import datetime
from .widgets import LocalWidgetBase
from .base_lib import JsonHandler


class GreetingWidget(LocalWidgetBase):
    WIDGET_NAME    = "问候语"
    NEED_CACHE     = True
    LOCAL_INTERVAL = "1h"  # 一小时缓存，小时级变化

    def _fetch_data(self) -> dict:
        hour = datetime.now().hour
        greeting = ("夜深了" if hour < 6 else
                    "早上好" if hour < 12 else
                    "下午好" if hour < 18 else
                    "晚上好")
        name = JsonHandler().read("General", "user_name") or "朋友"
        return {"greeting": greeting, "name": name}
```

调用：

```python
w = GreetingWidget()
data = w.get_data()
data = w.get_data(force_refresh=True)  # 跳过缓存
await w.get_data_async()                # 异步调用
```

### 2. 联网组件 —— 一言（公开 API，无需 Key）

```python
from .widgets import NetworkWidgetBase


class HitokotoWidget(NetworkWidgetBase):
    WIDGET_NAME    = "一言"
    NEED_CACHE     = True
    LOCAL_INTERVAL = "1d"          # 一天只请求一次
    API_URL        = "https://v1.hitokoto.cn/"

    def _parse_data(self, raw: dict) -> dict:
        return {
            "hitokoto": raw.get("hitokoto", ""),
            "from":     raw.get("from", ""),
            "from_who": raw.get("from_who", ""),
        }
```

调用：

```python
h = HitokotoWidget()
data = h.get_data()
# 当天再次调用直接返回缓存，不发网络请求
```

---

## 缓存系统

### 基本配置

组件上声明三个类变量即可：

| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `WIDGET_NAME` | `str` | `"StartInfo组件"` | 组件唯一标识，用于数据库主键 |
| `NEED_CACHE` | `bool` | `False` | 是否启用缓存 |
| `LOCAL_INTERVAL` | `str` | `"1s"` | 缓存有效期，支持 `"1s"` `"5m"` `"2h"` `"1d"` |

### 多缓存键

同一组件可管理多个独立缓存，使用 `cache_key` 区分：

```python
# 可以从 API 返回中提取不同部分，分别缓存
class WeatherWidget(NetworkWidgetBase):
    def _fetch_data(self) -> dict:
        raw = self._sync_request()
        self._save_cache(raw.get("weather", {}), cache_key="weather")
        self._save_cache(raw.get("aqi", {}),     cache_key="aqi")
        return raw

    def get_weather(self):
        return self.get_data(cache_key="weather")

    def get_aqi(self):
        return self.get_data(cache_key="aqi")
```

### JSON 路径读写

利用 SQLite 原生的 `json_extract` / `json_set` / `json_remove`，支持按路径操作缓存，不缓存整个 dict 到 Python 内存。

```python
# 路径写法：点分，如 "weather.temp" 或 "$."weather"."temp""
value, ts = widget._read_cache_path("weather.temp")
widget._update_cache_path("weather.temp", 30)
widget._remove_cache_path("weather")
```

**优势：** 更新一个叶子节点时，SQL 层面只写一行，不需要把整个 dict 读出 → Python 改 → 重新写入。

### 过期策略详解

| `NEED_CACHE` | `LOCAL_INTERVAL` | 行为 |
|:---:|:---:|---|
| `False` | 任意 | 每次调用 `get_data()` 直接执行 `_fetch_data()` |
| `True` | `"1h"` | 缓存写入后 1 小时内返回缓存，过期后重新获取 |
| `True` | `"0"` 或 `"0s"` | 缓存永不过期，写入后永远返回缓存（除非 `force_refresh=True`） |
| `True` | `"1d"` | 每天更新一次 |

`force_refresh=True` 在任何情况下都跳过缓存判断，强制 `_fetch_data()`。

### 跳过缓存

当数据获取失败（网络超时、API 返回错误）时，不应该把错误数据写入缓存。在 `_parse_data` 或 `_fetch_data` 中调用 `self.skip_cache()` 即可：

```python
class WeatherWidget(NetworkWidgetBase):
    def _parse_data(self, raw_data: dict) -> dict:
        now = raw_data.get('now')
        if not now:
            self.skip_cache()       # 失败数据不缓存
            return {}
        return {...}
```

调用 `skip_cache()` 后，本次 `get_data()` 只返回数据但不写入数据库，下次调用仍然会重新获取。

---

## 异步模式

`NetworkWidgetBase` 提供两套异步方案：

### 短连接（默认）

```python
async def _async_request(self) -> dict:
    async with httpx.AsyncClient(timeout=5.0) as client:
        ...
```

- 每次调用创建新的连接池，请求结束自动关闭
- 零心智负担，适合绝大多数组件（小时/天级刷新）
- `get_data_async()` 默认使用此模式

### 长连接（高频场景）

当组件需要短周期高频轮询（如定时器每 30 秒一次）时，切换为长连接避免反复握手：

```python
class HighFreqWidget(NetworkWidgetBase):
    # 只需覆写 _fetch_data_async 改用长连接方法
    async def _fetch_data_async(self) -> dict:
        return await self._async_request_persistent()
```

注意事项：

- 长连接复用 `self._async_client`，在组件生命周期内保持连接池
- 不再使用时必须调用 `await widget.close()` 释放资源
- 可在窗口销毁事件（如 Qt 的 `closeEvent`）或定时器停止时调用

```python
class MyWindow(QMainWindow):
    async def closeEvent(self, event):
        await self.weather_widget.close()
        super().closeEvent(event)
```

---

## API 参考

### WidgetBase

**类变量（子类覆写）：**

| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `WIDGET_NAME` | `str` | `"StartInfo组件"` | 组件标识，数据库主键的一部分 |
| `NEED_CACHE` | `bool` | `False` | 是否启用缓存 |
| `LOCAL_INTERVAL` | `str` | `"1s"` | 缓存有效期 |

**公共方法：**

| 方法 | 返回 | 说明 |
|------|------|------|
| `get_data(*, force_refresh=False, cache_key="default")` | `dict` | 同步获取数据，自动处理缓存 |
| `get_data_async(*, force_refresh=False, cache_key="default")` | `dict` | 异步获取数据，自动处理缓存 |
| `skip_cache()` | `None` | 标记本次获取的数据不写入缓存 |

**缓存辅助方法（通常不需要直接调用）：**

| 方法 | 返回 | 说明 |
|------|------|------|
| `_read_cache(cache_key)` | `(dict, float) \| None` | 读整条缓存 + 时间戳 |
| `_save_cache(data, cache_key)` | `bool` | 写入整条缓存 |
| `_clear_cache(cache_key)` | `bool` | 删除缓存 |
| `_read_cache_path(json_path, cache_key)` | `(Any, float) \| None` | 读路径节点 + 时间戳 |
| `_update_cache_path(json_path, value, cache_key)` | `bool` | 局部更新路径值 |
| `_remove_cache_path(json_path, cache_key)` | `bool` | 删除路径子树 |

**子类覆写点：**

| 方法 | 返回 | 说明 |
|------|------|------|
| `_fetch_data()` | `dict` | 同步获取原始数据（必须覆写） |
| `_fetch_data_async()` | `dict` | 异步获取（默认回退到同步） |

### LocalWidgetBase

继承 `WidgetBase`，不新增属性/方法。子类只需覆写 `_fetch_data()`。

### NetworkWidgetBase

**额外类变量：**

| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `API_URL` | `str` | `""` | API 地址（必须设置） |
| `RETRY_COUNT` | `int` | `2` | 失败后重试次数（总共请求 count+1 次） |
| `RETRY_DELAY` | `float` | `1.0` | 重试间隔（秒） |
| `REQUEST_TIMEOUT` | `float` | `5.0` | 单次请求超时时间（秒） |

**公共方法：**

| 方法 | 返回 | 说明 |
|------|------|------|
| `close()` | `None` | 关闭长连接的 AsyncClient |

**子类覆写点：**

| 方法 | 返回 | 说明 |
|------|------|------|
| `_parse_data(raw_data)` | `dict` | 解析 API 返回的 JSON（必须覆写） |

**类变量（子类直接覆写，和 `API_URL` 同级）：**

| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `PARAMS` | `dict \| None` | `None` | 请求参数字典 |
| `HEADERS` | `dict \| None` | `None` | 请求头字典 |

**内部方法：**

| 方法 | 返回 | 说明 |
|------|------|------|
| `_sync_request()` | `dict` | 同步 GET（`httpx.Client`） |
| `_async_request()` | `dict` | 短连接异步（默认，`async with` 用完即关） |
| `_async_request_persistent()` | `dict` | 长连接异步（复用连接池，需 `close()`） |
| `_get_async_client()` | `AsyncClient` | 懒加载复用客户端 |

`_fetch_data_async()` 默认走 `_async_request()`（短连接）。高频场景覆写改为 `_async_request_persistent()`。

### MultiSourceWidgetBase

继承 `NetworkWidgetBase`，用于同一组件有多个数据源可选、由用户配置切换的场景（如每日一言可以选择 iciba 或 hitokoto）。

**类变量（子类必须覆写）：**

| 属性 | 类型 | 说明 |
|------|------|------|
| `SOURCES` | `dict[str, SourceConfig]` | 所有可用数据源，key 为数据源名称 |
| `CONFIG_ITEM` | `ConfigItem` | 关联配置项，值决定当前使用哪个数据源 |

`SourceConfig` 字段（`TypedDict`，均可选）：

| 字段 | 类型 | 说明 |
|------|------|------|
| `url` | `str` | API 地址 |
| `params` | `dict \| None` | 请求参数字典 |
| `headers` | `dict \| None` | 请求头字典 |
| `parse_func` | `Callable` | 解析函数名（字符串，对应子类方法） |

**只读属性（由 SOURCES + CONFIG_ITEM 自动推导）：**

| 属性 | 说明 |
|------|------|
| `DATA_SOURCE` | 当前选中的数据源名称（`CONFIG_ITEM.value`） |
| `API_DATA` | 当前数据源的完整配置 |
| `API_URL` / `PARAMS` / `HEADERS` | 自动从 `API_DATA` 取对应字段 |
| `PARSE_FUNC` | 当前数据源的解析函数（已绑定为子类方法） |

**子类覆写点：**

- 为每个数据源写一个解析方法，方法名与 `SOURCES` 中的 `parse_func` 对应
- 解析方法接收 API 原始 JSON，返回 `dict`

**公共方法：**

| 方法 | 返回 | 说明 |
|------|------|------|
| `get_cached_source()` | `str` | 读取已缓存数据来自哪个数据源 |

**使用示例：**

```python
class EveryDayWordsWidget(MultiSourceWidgetBase):
    WIDGET_NAME    = 'EveryDayWords'
    NEED_CACHE     = True
    LOCAL_INTERVAL = '1d'
    CONFIG_ITEM    = cfg.words_source            # OptionsConfigItem，选项如 ['iciba', 'hitokoto']
    SOURCES = {
        'iciba': {
            'url': 'https://open.iciba.com/dsapi/',
            'parse_func': '_parse_iciba',
        },
        'hitokoto': {
            'url': 'https://v1.hitokoto.cn/',
            'parse_func': '_parse_hitokoto',
        },
    }

    def _parse_iciba(self, raw_data: dict) -> dict:
        return {'words_primary': raw_data.get('note', '')}

    def _parse_hitokoto(self, raw_data: dict) -> dict:
        return {'words_primary': raw_data.get('hitokoto', '')}
```

调用方式与普通联网组件一致：`widget.get_data()` / `await widget.get_data_async()`。解析结果会自动附带 `source` 字段记录数据源名称。

**注意：** 缓存中存有 `source` 字段，切换数据源后旧的缓存不会自动失效——如需切换后强制刷新，调用 `get_data(force_refresh=True)` 或先 `clear_cache()`。

### CacheManager

进阶使用，通常不需要直接调用。所有方法均通过 `WidgetBase` 的封装调用。

| 类方法 | 说明 |
|--------|------|
| `init_db()` | 初始化数据库（幂等，`WidgetBase.__init__` 已调用） |
| `read_path(w, ck, path)` | `json_extract` 路径读取 |
| `update_path(w, ck, path, value)` | `json_set` 路径写入 |
| `remove_path(w, ck, path)` | `json_remove` 路径删除 |

详情见 [JSON 路径读写](#json-路径读写) 章节。

---

## 从旧版迁移

旧版 `core/get_data.py` 使用独立函数 + `format_data_to_json` / `format_data_to_jinja2` 手动管理缓存。迁移至组件框架后的变化：

| | 旧版 | 新版 |
|---|---|---|
| 数据获取 | 每个模块一个独立函数 | 一个类，覆写 `_fetch_data` |
| 网络请求 | 手动 `aiohttp` + session 管理 | 框架封装 `httpx`，自动处理 |
| 缓存 | 手动 `data.json` 读写 | SQLite 自动缓存，按 cache_key / 路径管理 |
| 异常处理 | 每函数写 `try/except/return False` | 框架统一抛出，调用方决定如何处理 |
| 类型安全 | 返回 `dict \| bool`（易忘判假） | 返回 `dict`，失败则抛异常 |

迁移步骤：

1. 新建组件类，继承 `LocalWidgetBase` 或 `NetworkWidgetBase`
2. 从原函数的业务逻辑中提取数据获取/解析代码放入 `_fetch_data` 或 `_parse_data`
3. 配置 `NEED_CACHE` 和 `LOCAL_INTERVAL`
4. 调用方统一用 `widget.get_data()` / `await widget.get_data_async()`

---

## FAQ

**Q：缓存数据库文件在哪？**

`DB/widgets_cache.db`（项目根目录下）。`CacheManager.init_db()` 在 `WidgetBase.__init__()` 中自动调用。

**Q：缓存数据和 `data.json` 是什么关系？**

两者独立。组件缓存（`widgets_cache.db`）替代了旧的 `data.json` 缓存层。`data.json` 继续用于持久化用户配置和设置数据（`JsonHandler`）。

**Q：组件怎么获取 API Key？**

`NetworkWidgetBase` 不强制 key 管理方式。子类设置 `PARAMS` 即可：

```python
class MyWidget(NetworkWidgetBase):
    PARAMS = {"key": cfg.my_api_key.value}
```

**Q：异步方法兼容 PySide6 + qasync 吗？**

完全兼容。`ui.py` 中将全局事件循环设置为 `QEventLoop`，`httpx.AsyncClient` 底层走标准 asyncio 接口，对 qasync 透明。

**Q：同一个组件可以同时有同步和异步调用吗？**

可以。`get_data()` 用于同步上下文，`get_data_async()` 用于异步上下文，两者共享同一套缓存。不推荐混用，但技术上可行。

**Q：怎么配置自动重试？**

设置 `RETRY_COUNT` 和 `RETRY_DELAY` 类变量：

```python
class MyWidget(NetworkWidgetBase):
    RETRY_COUNT = 3      # 失败后重试 3 次（总共请求 4 次）
    RETRY_DELAY = 2.0    # 每次重试间隔 2 秒
```

- 只重试 `httpx.HTTPError`（网络/HTTP 错误），不重试代码 bug（`Exception`）
- 默认 `RETRY_COUNT=2`（共 3 次尝试），`RETRY_DELAY=1.0` 秒
- 同步重试用 `time.sleep`，异步重试用 `asyncio.sleep`，都不阻塞各自的事件循环
- 三次方法 `_sync_request`、`_async_request`、`_async_request_persistent` 均享有重试逻辑

---

> 文档版本 1.2 · 维护者：HuangTao · 最后更新：2026-08-10
